# Resources required by the sample browser and most samples.
[Essential]

# Common sample resources needed by many of the samples.
# Rarely used resources should be separately loaded by the
# samples which require them.
[Popular]
FileSystem=@OGRE_MEDIA_DIR_REL@/scripts/Compositors
FileSystem=@OGRE_MEDIA_DIR_REL@/scripts/materials/Pbsmaterials
FileSystem=@OGRE_MEDIA_DIR_REL@/models
FileSystem=@OGRE_MEDIA_DIR_REL@/textures

# Do not load this as a resource. It's here merely to tell the code where
# the Hlms templates are located
[Hlms]
DoNotUseAsResource=@OGRE_MEDIA_DIR_REL@

[Mygui]
FileSystem=@OGRE_MEDIA_DIR_REL@/mygui/
